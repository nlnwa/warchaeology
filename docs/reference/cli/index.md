---
title: Usage
---

The tool binary is named `warc`.

## Syntax

```text
warc [command [subcommand]] [flags]
```

- `command` and optional `subcommand` select the operation
- `flags` configure execution behavior

The pages in this section are generated from built-in CLI help.
